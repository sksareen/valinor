node_modules/
.DS_Store
*.webm

# secrets / keys — never commit (belt & suspenders; real config lives in .env)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
*.keystore

handviz-watchdog.log
.tmp-activity-cursor.png
.tmp-activity-cursor.jpg

# editor / local tooling
.vscode/
.cursor/
.vercel/
.obsidian/

# Loopkeeper (bundled FastAPI)
loopkeeper/venv/
loopkeeper/__pycache__/
loopkeeper/**/__pycache__/
loopkeeper/.env
loopkeeper/*.db
loopkeeper/design_runs/
loopkeeper/design_runs/**

# Kitten TTS (local neural voice for live companion)
tts/venv/
tts/models/
tts/tmp/
tts/__pycache__/
tts/**/__pycache__/

# personal message content / contact enrichment — never commit
drafts.json
crm.json
message-profile.json
live-session.jsonl
live-conversations.json
agent-usage.jsonl
rehearse-sessions.jsonl

# personal writing — keep the folder, not the letters
letters/*
!letters/.gitkeep
letters/.history/

# writing voice model — personal exemplars + distilled profile; never promote to valinor
writing-voice.md
writing-stars.json
writing-samples.json
writing-projects.json
writing-voice-runs.json
letter-session-prompt-runs.json

# execute planning engine — personal prompt tuning + planning trajectories
execute-plan-prompt.json
execute-plan-runs.json

# Pi Live runtime cache (isolated agentDir; do not use ~/.pi)
valinor-agent/.runtime/
